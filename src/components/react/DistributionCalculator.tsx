import DiceInputSegment from "./DiceInputSegment";
import InputField from "./InputField";

const DistributionCalculator = () => {
  return (
    <div>
      <DiceInputSegment></DiceInputSegment>
    </div>
  );
};

export default DistributionCalculator;
